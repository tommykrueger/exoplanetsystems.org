/**
 * Math functions that are useful to calculate bodies in space
 */

window.spaceMath = {

	toRad: function( a, b ) {
	  return a + Math.random() * ( b - a );
	},

	toDeg: function() {

	}

}







